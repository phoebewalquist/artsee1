import "./EventDetail.css";

// Used to display the details of any order, including the cart (unpaid order)
export default function EventDetail({ event }) {
  return <div className="EventDetail"></div>;
}
